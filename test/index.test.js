const { expect } = require("chai");

describe("todo", () => {
  it("todo", () => {
    expect(aVariable).to.equal("hello");
  });

  it("todo", () => {
    expect(anotherVariable).to.equal("goodbye");
  });
});
